import React from "react";
import { data } from "../data/data";
const Work = () => {
  // projects file
  const project = data;
  //setProject(data);

  return <div className='w-full md:h-screen text-gray'></div>;
};

export default Work;
